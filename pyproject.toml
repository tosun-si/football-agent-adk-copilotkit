[project]
name = "football-agent-adk-antigravity"
version = "0.1.0"
description = "Football statistics agent using Google ADK with BigQuery MCP via Cloud API Registry"
requires-python = ">=3.11"
dependencies = [
    "google-adk>=1.27.2",
    "google-cloud-aiplatform>=1.141.0",
]

[dependency-groups]
dev = [
    "python-pptx>=1.0.2",
]
